# Environment Variables for Phonebook Backend

# MongoDB connection URI (for Render, use MongoDB Atlas connection string)
# Example: mongodb+srv://username:password@cluster.mongodb.net/phonebook?retryWrites=true&w=majority
MONGODB_URI=

# Port for the server (Render automatically sets this, but defaults to 3001 for local development)
PORT=3001

# Node environment (set to 'production' in Render dashboard)
NODE_ENV=development
