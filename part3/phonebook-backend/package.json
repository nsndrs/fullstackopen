{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "Phonebook backend for Full Stack Open part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook/phonebook && npm run build && cp -r dist ../../phonebook-backend",
    "deploy": "npm run build:ui && git add . && git commit -m 'Updated frontend' && git push",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'Updated frontend' && git push && npm run deploy"
  },
  "author": "Nick Sanders",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.16.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
