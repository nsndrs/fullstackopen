# Use the SAME MongoDB connection as part3 phonebook-backend
# Copy this value from your part3/.env file:
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/phonebook?retryWrites=true&w=majority

# Different port to avoid conflict with part3 (which uses 3001)
PORT=3003
NODE_ENV=development
